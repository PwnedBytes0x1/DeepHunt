# DeepHunt skills package
# Skills can be loaded from:
#   1. WORKSPACE_DIR/skills/ (user-defined)
#   2. WORKSPACE_DIR/skills/<category>/<skill>.md (nested)
#   3. bundled skills (this package)
"""

Skills are Markdown-based skill definitions that can be loaded dynamically.
Each skill file contains YAML frontmatter with metadata and Markdown content.
"""
